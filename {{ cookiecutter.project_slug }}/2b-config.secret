apiVersion: v1
kind: Secret
metadata:
  namespace: {{ cookiecutter.namespace }}
  name: {{ cookiecutter.project_slug }}-env-secret
  labels:
    app.kubernetes.io/name: {{ cookiecutter.project_slug }}-env-secret
type: Opaque
data:
  VARIABLE: <echo -n "VALUE" | base64 >

